/**
 * @format
 * @flow
 */

import { moderateScale as normalize } from 'react-native-size-matters'

import colors from './colors'
import fontFamily from './fonts'

export default {
  colors,
  fontFamily,
  normalize,
}
export { colors, fontFamily, normalize }
